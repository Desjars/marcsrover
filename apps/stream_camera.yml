nodes:
  - id: zenoh_receive
    path: uv
    args: run ../nodes/zenoh_receive.py
    outputs:
      - image

  - id: plot
    path: uv
    args: run ../nodes/opencv_plot.py
    inputs:
      image:
        source: zenoh_receive/image
        queue_size: 1
